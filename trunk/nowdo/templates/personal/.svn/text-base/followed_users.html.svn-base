{% extends "personal/base.html" %}

{% block extra_js %}
    <script type="text/javascript">
        seajs.use(['nowdo', '$'], function(nowdo, $){
            $(function(){
                nowdo.trigger.addToggleFollowUserTrigger('.follow-user');
            });
        });
    </script>
{% endblock %}

{% block content %}
    {{ macros.my_home_nav(owner) }}

    {% call macros.render_panel_content() %}
        {{ macros.render_user_list(followed_users_pagination.object_list, large_size=True) }}
    {% endcall %}
    {{ macros.render_pagination(followed_users_pagination, 'personal.followed_users', owner) }}

{% endblock %}