{% extends 'layout/layout.html' %}
{% block content %}
    {% call macros.render_panel_content('创建新的小组') %}
        <form action="{{ url_for('group.create_group') }}" method="post">
            {{ form.csrf_token }}
            <div class="form-group">
                {{ form.group_name.label }}
                {{ form.group_name(class="form-control", placeholder="请填写小组名称") }}
            </div>
            <div class="form-group">
                {{ form.group_description.label }}
                {{ form.group_description(class="form-control", placeholder="请填写小组描述") }}
            </div>
            <div class="form-group">
                {{ form.group_tags.label }}
                {{ form.group_tags(class="form-control", placeholder="请填写小组标签") }}
                <p class="help-block">{{ _("小组标签是描述小组的内容。标签作为关键词可以被用户搜索到。 多个标签之间用“，”分隔开。") }}</p>
            </div>
            {% if form.errors %}
                <div class="alert alert-danger">
                    {% for label, error_list in form.errors.iteritems() %}
                        <p>{{ error_list[0] }}</p>
                    {% endfor %}
                </div>
            {% endif %}
            <div class="form-group">
                <button type="submit" class="btn btn-primary">提交</button>
                <a class="btn btn-default" href="{{ url_for('frontend.index') }}">返回</a>
            </div>
        </form>
    {% endcall %}
{% endblock %}

{% block sidebar %}
    {% call macros.render_panel_xc('友情提示') %}
        <div class="panel-body">
            考虑到中国法律法规和相关政策的要求,脑洞不欢迎色情、激进话题、意识形态方面的讨论, 并保留解散这类主题小组的权利。
        </div>
    {% endcall %}
{% endblock %}
