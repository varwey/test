{% extends "account/base/account_layout.html" %}

{% block title %}
    <title>{{ _("重置密码 | 协作翻译平台") }}</title>
{% endblock %}

{% block content %}
    <div class="login-form-wrapper">
    <div class="container login-container">
        <form id="command" class="form largeGenericForm form-signin" action="{{ url_for('account.reset_pw') }}" method="post">
            {{ form.csrf_token }}
            {{ form.id() }}
            {{ form.key() }}
            <div class="form-group {% if form.errors.email %} has-error has-feedback {% endif %}">
                <a class="pull-right" href="{{ url_for('account.login') }}">立即登录</a>
                {% if form.errors.email %}
                    <label class="control-label" for="email">{{ form.errors.email[0] }}</label>
                {% endif %}
                {{ form.email(class="form-control input-lg", placeholder=form.email.description) }}
                {% if form.errors.email %}
                    <span class="glyphicon glyphicon-remove form-control-feedback feedback-lg"></span>
                {% endif %}
            </div>
            <div class="form-group {% if form.errors.password %} has-error has-feedback {% endif %}">
                {% if form.errors.password %}
                    <label class="control-label" for="email">{{ form.errors.password[0] }}</label>
                {% endif %}
                {{ form.password(class="form-control input-lg", placeholder=form.password.description) }}
                {% if form.errors.password %}
                    <span class="glyphicon glyphicon-remove form-control-feedback feedback-lg"></span>
                {% endif %}
            </div>
            <div class="form-group {% if form.errors.confirm_password %} has-error has-feedback {% endif %}">
                {% if form.errors.confirm_password %}
                    <label class="control-label" for="email">{{ form.errors.confirm_password[0] }}</label>
                {% endif %}
                {{ form.confirm_password(class="form-control input-lg", placeholder=form.confirm_password.description) }}
                {% if form.errors.confirm_password %}
                    <span class="glyphicon glyphicon-remove form-control-feedback feedback-lg"></span>
                {% endif %}
            </div>
            <button type="submit" class="btn btn-success btn-lg btn-block">{{ _('提交') }}</button>
        </form>
    </div>
    </div>
{% endblock %}
