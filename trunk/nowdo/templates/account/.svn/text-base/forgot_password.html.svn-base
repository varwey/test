{% extends "account/base/account_layout.html" %}

{% block title %}
    <title>{{ _("找回密码 | 协作翻译平台") }}</title>
{% endblock %}

{% block content %}
    <div class="login-form-wrapper">
        <div class="container login-container">
            <form role="form" class="form-signin" action="{{ url_for('account.forget_password') }}" method="post">
                {{ form.csrf_token }}
                <div class="form-group {% if form.errors.email %} has-error has-feedback {% endif %}">
                    <a class="pull-right" href="{{ url_for('account.login') }}">立即登录</a>
                    {% if form.errors.email %}
                        <label class="control-label" for="email">{{ form.errors.email[0] }}</label>
                    {% endif %}
{#                    <label class="control-label" for="email">{{ form.email.label }}</label>#}
                    {{ form.email(class="form-control input-lg", placeholder="注册邮箱") }}
                    {% if form.errors.email %}
                        <span class="glyphicon glyphicon-remove form-control-feedback"></span>
                    {% endif %}
                </div>
                <button class="btn btn-success btn-lg btn-block" type="submit">{{ _('重设密码') }}</button>
            </form>
        </div>
    </div>
{% endblock %}